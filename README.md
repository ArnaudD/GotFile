GotFile allows you to store your dot file in a git repository

Use case :

    gotfile setup http://github.com/ArnaudD/myDotFiles
    gotfile add .svn
    gotfile add .bashrc
    gotfile rm .mozilla
    gotfile sync

